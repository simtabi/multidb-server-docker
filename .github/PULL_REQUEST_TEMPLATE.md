## What this changes

<!-- One or two sentences. What behaviour is different afterwards? -->

## Why

<!-- The problem, not the patch. If it fixes a bug, what was the symptom, and
     why did it point somewhere other than the cause? -->

## How it was verified

- [ ] `make lint` is clean
- [ ] `make verify-structure` passes
- [ ] The checks touching this area pass, named here:
- [ ] A new or changed check was **negative-controlled** — it fails when the
      thing it asserts is broken, and passes when it is not

<!-- A check that has never been seen to fail is not evidence. -->

## Anything deliberately left out

<!-- Scope you chose not to cover, and why. Say it here rather than leaving it
     to be discovered. -->
